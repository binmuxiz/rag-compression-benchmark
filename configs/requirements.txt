# NGC PyTorch 23.10 이미지의 사전 설치 패키지와 맞춘 버전
# - numba 0.57.1: numpy>=1.21,<1.25
# - cupy-cuda12x 12.1.0: numpy>=1.20,<1.27
# - matplotlib/contourpy: numpy<2
numpy==1.24.4

# spacy 3.7.1 / weasel 0.3.2 호환
typer==0.9.0

# FAISS — aarch64(Jetson) 미지원인 GPU 대신 CPU 버전 사용
# x86 H200 환경과 ARM 온디바이스 환경 모두 동일 코드 경로 유지
faiss-cpu

# 임베딩/LLM 로딩
# 5.x는 torch>=2.4 요구 → NGC의 torch 2.1과 충돌하므로 4.x로 고정
transformers==4.46.0
datasets
sentence-transformers
tqdm

